# ImaginaChingon.mx

## Development

Install the dependencies:

```sh
bundle install
```

Then serve the site:

```sh
# This will serve the site at http://127.0.0.1:4000/
bundle exec jekyll serve
```
